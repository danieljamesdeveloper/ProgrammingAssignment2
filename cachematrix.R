## Put comments here that give an overall description of what your
## functions do


# Definesa the get and set methods, and get and set inverse methods. Creates the cache as a list.

makeCacheMatrix <- function(x = matrix()) {

}


# Returns from the argument value from the cache if it exists, otherwise
# re-calculates, sets the cache, and retruns the recalculated value

cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
}